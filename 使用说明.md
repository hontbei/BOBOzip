# UnzipTool 使用说明

## 这次改了什么

1. 使用 `CustomTkinter` 重做界面，整体更现代
2. 密码列表改成卡片块状展示，不再是傻乎乎的一整坨文本框
3. 新增可视化进度条、当前处理状态、统计信息
4. 支持单个添加、批量导入、删除密码、自动保存密码库
5. 保留原有核心能力：批量解压 ZIP / RAR / 7Z、自动尝试密码、成功后删除压缩包与分卷、写日志

## 项目目录

- `unzip_gui.py`：图形界面入口
- `unzip_core.py`：解压、扫描、删除、日志等核心逻辑
- `unzip_config.json`：密码配置
- `requirements.txt`：依赖
- `tests/test_unzip_core.py`：核心逻辑测试

## 安装依赖

```bash
pip install -r requirements.txt
```

如果要解压 RAR：
- Windows 需安装 UnRAR 或 WinRAR/UnRAR 可执行工具

如果要处理某些分卷 ZIP：
- 建议安装 7-Zip，并确保 `7z.exe` 可用

## 启动方式

优先推荐直接双击：

- `启动UnzipTool.bat`

它会自动：
1. 检测 Python
2. 检查并安装依赖
3. 启动 `unzip_gui.py`

命令行启动也可以：

```bash
python unzip_gui.py
```

## 使用方式

1. 选择要扫描的文件夹
2. 可以先点“扫描压缩包”查看待处理数量
3. 在右侧密码区添加密码，或批量导入
4. 点击“开始解压”
5. 观察进度条、当前状态、日志输出
6. 成功解压后会自动删除对应压缩包和分卷
7. 处理结束后会在目标目录生成 `unzip_log_时间戳.txt`

## 密码批量导入格式

支持：
- 每行一个密码
- 逗号分隔
- 中文逗号也行

例如：

```text
123456
hello_world
abc,def，ghi
```

## 测试

项目自带基础单元测试：

```bash
python -m unittest tests/test_unzip_core.py
```

## 提醒

1. 解压成功后会删除压缩包，请先备份重要文件
2. 如果依赖没装齐，RAR / 7Z 功能会报缺少依赖
3. 如果 7-Zip 没安装，分卷 ZIP 的兜底方案无法使用
4. Windows 下运行这个 GUI 最合适，别拿 WSL 的图形支持折腾自己
